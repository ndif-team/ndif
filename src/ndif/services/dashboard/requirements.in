# Pinned to match the parent ndif package (ndif/pyproject.toml) so the two
# installs don't fight over fastapi/uvicorn versions.
fastapi==0.108.0
uvicorn[standard]==0.24.0
pydantic-settings>=2.1.0
itsdangerous>=2.1.0
bcrypt>=4.0.0
python-multipart>=0.0.6
requests
nnsight @ git+https://github.com/ndif-team/nnsight.git@hackathon/peft
peft
# Required by transformers' VLM image processors (e.g. Gemma3ImageProcessor).
# Without it, AutoImageProcessor lookups for VisionLanguageModel checkpoints
# fail at get_model_key time inside the dashboard / reconcile path.
torchvision
# The reconcile cron drives deploy/evict/restart through the cli lib, which
# uses the Ray client. Pinned to match services/ray/requirements.in.
ray[client]==2.55.1
# cli.lib.checks.check_redis + cli.lib.util.notify_reconcile need a redis client.
redis
