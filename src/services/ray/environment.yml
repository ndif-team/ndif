channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - git
  - pip:
    - pip
    - setuptools
    - nnsight
    - ray[serve]==2.50.0
    - python-slugify
    # Telemetry
    - prometheus_client
    - python-logging-loki
    # Database
    - boto3
    - influxdb-client
    # Google Cal Scheduling
    - google-api-python-client
    - google
    - einops
