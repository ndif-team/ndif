channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - git
  - pip:
    - pip
    - setuptools
    - nnsight
    - ray[serve]
    - python-slugify
    # Telemetry
    - prometheus_client
    - python-logging-loki
    # Database
    - minio
    - influxdb-client
    - google-api-python-client